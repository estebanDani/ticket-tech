import React from 'react'
import { Container, TextField, Typography } from '@mui/material'

const TheaterForm = () => {
    return (
        <Container maxWidth={false}>
            <Typography variant='h4'>Crear Sala</Typography>

        </Container>
    )
}

export default TheaterForm